# Log

The Log panel records application messages as you work: file loads, job submissions, completions, warnings, and errors. It's useful for tracking what happened during a session and for diagnosing unexpected behavior.

You can open the Log panel from `Window` :material-arrow-right: `Log Panel`.
